==== ex03 ====
ft_recursive_power
Turn-in directory: ex03/
Files to turn in: ft_recursive_power.c
Allowed functions: None
• Create a recursive function that returns the result of raising a number to a given
power.
• If the power is less than 0, the function should return 0.
• Overflows do not need to be handled; the function’s return value will be undefined
in such cases.
• By definition, 0 raised to the power of 0 should return 1.
• The function should be prototyped as follows:
int ft_recursive_power(int nb, int power);
******
int	ft_recursive_power(int nb, int power)
{
	int	result;

	result = 1;
	if (power < 0)
		return (0);
	if (nb == 0 || power == 0)
		return (1);
	result = nb * ft_recursive_power(nb, power - 1);
	return (result);
}

/*
#include <unistd.h>

void	ft_putnbr(int nbr)
{
	char	c;

	if (nbr == -2147483648)
	{
		write(1, "-2147483648", 11);
		return ;
	}
	if (nbr < 0)
	{
		nbr = -nbr;
		write(1, "-", 1);
	}
	if (nbr >= 10)
		ft_putnbr (nbr / 10);
	c = nbr % 10 + '0';
	write(1, &c, 1);
}

int	main(void)
{
	int	nb;
	int	power;

	nb = 3;
	power = 2;
	ft_putnbr(ft_recursive_power(nb, power));
	return (0);
}
*/
