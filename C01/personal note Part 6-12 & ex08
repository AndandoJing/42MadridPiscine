Part 6 - sorting + nested loops
void  ft_sort_int_tab(int *tab, int size);

bubble sort
outside: loop 1
inside: loop 2

===== ex08 =====

ft_sort_int_tab
Turn-in directory: ex08/
Files to turn in: ft_sort_int_tab.c
Allowed functions: None
• Create a function that sorts an array of integers in ascending order.
• The function takes two arguments: a pointer to an int and the number of elements
in the array.
• The function should be prototyped as follows:
void ft_sort_int_tab(int *tab, int size);

*************

My idea:
void    ft_sort_int_tab(int *tab, int size)
{
        int     i;
        int     j;
        int     temp;

        j = 0;
        i = 0;
        while (j <= size -1) ------------ missing j++ to loop
        {
                while (tab[i] > tab[i + 1])----------but if starts with i <= i + 1? it will skip, so should use if
                {
                        temp = tab[i];
                        tab[i] = tab[i + 1];
                        tab[i + 1] = temp; ---------------- missing i++, no loop here
                }
        }
}

#include <stdio.h>

int     main(void)
{
        int     tab;-------------should be array, like int  tab[3]

        tab[0] = 3;
        tab[1] = 1;
        tab[2] = 2;
        printf("before: ", tab);-----------no %d, tab is not used
        ft_sort_int_tab(tab, 3);
        printf("after: ", tab);-----------no %d, tab is not used
        return (0);
}

Wrong.
special attention:
we need to avoid the tab[i + 1] is out of size
when a array has size number of elements, its legal index is 0 to size -1;
when the codes visit tab[i + 1], we need to ensure that i + 1 <= size -1, which is i < size - 1
if else, when i == size -1, visiting tab[i + 1], turns tab[size], which is out of the index.

attention:
1. size is a int, not unsigned
2. don't visit tab[0] or tab[1]
3. if we don't write %d, printf don't know that we need to take tab as integer and print it


Attention for nominette:
1. while (...)
2. new line between definition of varibles and assignment of their values
3. operation always with space, 1space+space2

Let me do it again:

void        ft_sort_int_tab(int *tab, int size)
{
        int        j;
        int        i;
        int        temp;

        j = 0;
        while (j < size - 1)
        {
                i = 0;
                while (i < size - 1)
                {
                        if (tab[i] > tab[i + 1])
                        {
                                temp = tab[i];
                                tab[i] = tab[i + 1];
                                tab[i + 1] = temp;
                        }
                        i++;
                }
                j++;
        }
}

#include <stdio.h>

void        ft_sort_int_tab(int *tab, int size);

int        main(void)
{
        int        tab[3];

        tab[0] = 3;
        tab[1] = 1;
        tab[2] = 2;
        printf("before: %d\n", tab[0], tab[1], tab[2]);
        ft_sort_int_tab(tab, 3);
        printf("after: %d", tab[0], tab[1], tab[2]);
        return (0);
}


-------------------------
now is void    ft_sort_int_tab(int *tab, int size)
{
        int     i;
        int     j;
        int     temp;

        j = 0;
        while (j < size -1)
        {
                i = 0;
                while (i < size - 1)
                {
                        if (tab[i] > tab[i + 1])
                        {
                                temp = tab[i];
                                tab[i] = tab[i + 1];
                                tab[i + 1] = temp;
                        }
                        i++;
                }
                j++;
        }
}

#include <stdio.h>

int     main(void)
{
        int     tab[10];
        int     i;

        tab[0] = 3;
        tab[1] = 1;
        tab[2] = 2;
        tab[3] = 0;
        tab[4] = -1;
        tab[5] = 100;
        tab[6] = 74;
        tab[7] = 42;
        tab[8] = -42;
        tab[9] = -89;
        i = 0;
        while (i < 10)
        {
                printf("%d ", tab[i]);
                i++;
        }
        printf("\n");

        ft_sort_int_tab(tab, 10);
        i = 10;
        while (i < 10)
        {
                printf("%d ", tab[i]);
                i++;
        }
        return (0);
}

output: 
-----------------
tab[0] = 3;
        tab[1] = 1;
        tab[2] = 2;
        tab[3] = 0;
        tab[4] = -1;
        tab[5] = 100;
        tab[6] = 74;
        tab[7] = 42;
        tab[8] = -42;
        tab[9] = -89;

