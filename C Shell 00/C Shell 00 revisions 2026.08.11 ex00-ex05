So uploaded to finish the first 5 exercises and turns out they are not ok!!! : (
Here are some of the errors I did:
ex00: OK | ex01: KO | ex02: KO | ex03: KO | ex04: OK | ex05: OK
ex06, ex07, ex08, ex09, nothging turned in
Let's review them one by one:
ex00: OK
ex01: KO
  Better to have only one archive, the .tar one
  The tar archive needs to be opened, then to see if the permissions are correct.
  Need to change the date and time as they are required.
ex02: KO
  Even doesn't exist test0 and test2
  Outside the .tar archive, test6 has nothing to link, it should be linked with test0
  Better to have only one archive, the .tar one
  Need to change the date and time as they are required.
ex03: KO
  This one has been corrected by the staff team, so don't mind, just do it again with the new document guide.
ex04: OK
  Although there is a doubt about double points, does it mean : or ..
  Surely that files starts with .. won't show, but with : yes, so maybe we can avoid it later in the 3r try.
ex05: OK
  
=============
Now let's do again the first 5 exercises in Shell 00.

********* ex00 *********

git clone myrepositorydirection Shell00_2
turns out there is an error, so I tried again with:
  cd ~
  rm -rf Shell00_2
  git clone myrepositorydirection Shell00_2
Here explains:
  rm: remove
  -r: revursive, needed to delete a directory and everything inside it, without -r, rm refueses to touch directories at all
  -f: suppresses all of that and just deletes silently, without asking
  combined: rm -rf Shell00_2 means "delete the Shell00_2 folder and everything inside it, no questions asked, no confirmation, no errors shown even if something's oddly protected."
Now we get the empty repository in the local computer.
Although I created the z file and ex00 separately, I moved the z file inside of ex00 later. So nothing happened :)
  cd Shell00_2
  ls
  echo "Z" > z #my bad, should create ex00 first
  cat z #should show Z
  mkdir ex00 #my bad, should do it before, since now, with ls, there are ex00 and z inside Shell00_2, so I need to move the z file again.
  mv z ex00/ #here ok, and checked, also good
  cd ex00 #enter ex00 folder
  cat -e z #to see if there is a "Z" followed by a new line, because it should be "Z$" with this command

********* ex01 *********
mkdir ex01
ls #show ex00 and ex01
cd ex01
touch testShell00
wc -c testShell00 #to know the word counts of this file testShell00
echo -n "Hi there it's a pleasure to see you." > testShell00
wc -c testShell00 #to check if the word bytes are sufficient
  shows "36 testShell00", ok, not sufficient
echo -n "Hi there it's a pleasure to see you. 123" > testShell00
wc -c testShell00 #to check again, and it's ok
cat testShell00
  shows "Hi there it's a pleasure to see you. 123%
ls -l
  shows:
    "total 4
     -rw-r--r-- 1 myusername 2026_madrid 40 Aug 11 19:45 testShell00"
  which I need to correct the date and time, and for sure the permissions
chmod 455 testShell00 #to change the permissions to -r--r-xr-x
ls -l
  shows:
    "total 4
     -r--r-xr-x 1 myusername 2026_madrid 40 Aug 11 19:45 testShell00" #now need to change the date and time
touch -t 202606012342 testShell00 #touch -t [[CC]YY]MMDDhhmm[.ss] filename
ls -l
  shows:
    "total 4
     -r--r-xr-x 1 myusername 2026_madrid 40 Jun  1 23:42 testShell00"

Now with the instruction, we continue with this:
tar -cf testShell00.tar testShell00 #to create the file to be submitted
  #tar: (Tape ARchive) utility 
  #-c: create
  #-f: file
  #-t: table of contents, list all files stored inside the archive.
  #-v: verbose, shows detailed metadata, like ls -l
  #-f: file
  #tar -cf <archive_name.tar> <file_or_folder_to_pack>
ls
  shows: testShell00 testShell00.tar #now need to remove the file testShell00
rm testShell00
  #need to confirm by the system, or can use rm -f testShell00, because -f means force
ls
  shows: testShell.tar #now it's ok, just need to check the content inside.
tar -tvf testShell00.tar
  shows: "-r--r-xr-x myusername/2026_madrid 40 2026-06-01 23:42 testShell00"

OK, finished.

************* ex02 ****************
mkdir ex02
cd ex02
mkdir test0 test2     #create test0 test2 as directories
touch test1 test3 test4 test5     # create the regular files
ln -s test0 test6     # create test6 as a symlink pointing to test0
    # add bytes to each one
echo -n "1234" > test1
echo -n "1" > test3
echo -n "12" > test4
echo -n "0" > test5
    # can create all in one command, using ; to separate:
      echo -n "1234" > test1; echo -n "1" > test3
      or
      using && to execute the next if the previous one is done successfully:
      echo -n "1234" > test1 && echo -n "1" > test3
    # change date for each one
touch -t 202606012047 test0
touch -t 202606012146 test1
touch -t 202606012245 test2
touch -t 202606012344 test3
touch -t 202606012343 test4
touch -t 202606012344 test5 
touch -t 202606012220 test6
    # to change the permission
chmod 715 test0
chmod 714 test1
chmod 504 test2
chmod 404 test3
chmod 641 test4
chmod 404 test5
    # the link test6 -> test0 can't be changed

    # to check
ls -l
total 16
drwx--xr-x 2 myusername 2026_madrid 6 Jun  1 22:20 test0
-rwx--xr-- 1 myusername 2026_madrid 4 Jun  1 21:46 test1
dr-x---r-- 2 myusername 2026_madrid 6 Jun  1 22:45 test2
-r-----r-- 1 myusername 2026_madrid 1 Jun  1 23:44 test3
-rw-r----x 1 myusername 2026_madrid 2 Jun  1 23:43 test4
-r-----r-- 1 myusername 2026_madrid 1 Jun  1 23:44 test5
lrwxrwxrwx 1 myusername 2026_madrid 5 Aug 11 20:31 test6 -> test0

    # can see that the date of the link is still wrong, because to change timestamp of a symbolic link itself, must add -h flag to touch.
touch -h -t 202606012220 test6

    #then can restore test0 timestamp
touch -t 202606012047 test0

    # can see that test3 and test5 have the same hard link number (which should be 2), attention, it's only for regular files, for directories it's not the same logic.
    # so it means that test3 and test5 are linked as well, but when we tried to link test3 and test5 (test5 already exist)

ln test3 test5
    # it will fail, because with an exist file, we can not link them, so we nened to delete/remove it first
rm test5
    # or with rm -f test5, by force
ln test3 test5
    # again we link them

touch -t 202606012344 test3
    # now check with ls -l we can see test3 and test5 are sharing the same hard link number, 2
ls -l
drwx--xr-x 2 jinjin 2026_madrid 6 Jun  1 20:47 test0
-rwx--xr-- 1 jinjin 2026_madrid 4 Jun  1 21:46 test1
dr-x---r-- 2 jinjin 2026_madrid 6 Jun  1 22:45 test2
-r-----r-- 2 jinjin 2026_madrid 1 Jun  1 23:44 test3
-rw-r----x 1 jinjin 2026_madrid 2 Jun  1 23:43 test4
-r-----r-- 2 jinjin 2026_madrid 1 Jun  1 23:44 test5
lrwxrwxrwx 1 jinjin 2026_madrid 5 Jun  1 22:20 test6 -> test0

    # now as the instructions indicete, we need to pack all files here into exo2.tar
tar -cf exo2.tar *
    # -c: create a new archive
    # -f exo2.tar: specifies the output file name (exo2.tar).
    # *: a shell wildecard that expands to match every file and directory in your current location (excluding hidden files that start with .).

    # since we only submit exo2.tar, need to remove all other files and directories
rm -rf test*
    # -r: if there are something inside, it must add -r
    # -f: force
    # test*: with * it means all files start with test
    # can also be done one by one, rm -rf test0, or rm test0 (need asking)

*************** ex03 ***************
mkdir ../ex03
    # if we are inside ex02 folder for example
cd ~
    # enter the home folder let's say
ssh-keygen -t ed25519
    # since the instructions indicate, create SSH key using the ed25519 algorithm, since with ssh-keygen, by default it will generate in type RSA
    # -t: Type
    # the key will be restored in ~/.ssh by default if we enter 3 times directly
cd ~/.ssh
ls -la
    # we will see all key documents here, one of them named like id_ed25519.pub
cat id_ed25519.pub
    # we see the public key directly

    # from now I tried to copy it and then remove it, but we can also just create the file that we need to submit
cp id_ed25519.pub ~/Shell00_2/ex03/id_ed25519.pub
    # command cp for copy
    # cp file1.txt file2.txt: copy file1.txt, name it as file2.txt
    # cp file1.txt ~/.ssh/ : copy file1.txt to the following folder
    # cp -r folder1 folder2 : -r is to copy and include all things inside folder1 to folder2

    # but what we want to submit is id_ed25519_pub, so we still need to write the content of the key to the new file and remove the id_ed25519.pub inside ex03 folder.
echo -n "thepublickey" > id_ed25519_pub
rm -f id_ed25519.pub

    # or, as I said, we can just create the correct file with correct name and the public key directly
cp ~/.ssh/id_ed25519.pub ~/Shell00_2/ex03/id_ed25519_pub

    # then just follow the instruction to update the SSH key in intranet.

**************** ex04 ****************
mkdir ../ex04 
    # create and enter ex04
    # can also be done with a combined command:
       mkdir -p ~/Shell00_2/ex04 && cd ~/Shell00_2/ex04
       -p : parent, means if the parent folder doesn't exist, it can be silently created
cd ../ex04
echo "ls -mtp" > midLS
    # ls -mtp is correct, no doubts
    # -m: formats output as a comma-and-space separeted list (file1, file2, dir1/)
          stmream format, make the default format as a horizental comma-and-space separated list
    # -t: from newest to oldest
    # -r: reverse, from the oldest to the newest
    # -p: make directory name with /, path/pathname indicator
    # -a: automatically ignores hidden files and directories starting with dot.
    # write the command WITH a trailing newline (do NOT use -n)
      in Unix/Linux, shell scripts and command files are expected to terminate with a newline character. When Moulinette evaluates your submission by executing sh midLS or bash midLS, a file missing a trailing newline can cause execution error or POSIX compliance failures.

chmod +x midLS
    # give execution permissions (standard practice at 42)

************** ex05 *****************
mkdir -p ~/Shell00_2/ex05 && cd ~/Shell00_2/ex05
echo 'git log -n 5 --format="%H"' >git_commit.sh


************** git add, git commit, git push, git status *************
always remember to do the git push with frequency

git add foldername 
or
git add .
    # will include all recent created folders and files


